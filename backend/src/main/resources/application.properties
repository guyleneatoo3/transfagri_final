spring.application.name=TRANSFAGRI
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/Cnef_Cameroun}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.session.jdbc.initialize-schema=always
#server.address=192.168.2.60
server.port=8082

# External API keys (use environment variables in production)
mistral.api.key=w0hMXfxmRC29Tzpupok8k0bpGMUEm4ze

# JWT secret (32+ chars). Override with env var JWT_SECRET in production.
jwt.secret=${JWT_SECRET:change-me-please-change-me-please-change-me-please}

# Répertoire d’upload pour FileStorageService
file.upload-dir=uploads
